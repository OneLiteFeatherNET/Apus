// Verdigris: oxidised copper, the tenant application's accent. See layers/design's tokens.css
// for why, and apps/console/app/app.config.ts for the other half of the pair.
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'verdigris',
      neutral: 'basalt'
    }
  }
})
